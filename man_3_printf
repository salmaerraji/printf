.TH _printf 3 "16 October 2023" "1.0" "_printf man page"
.SH NAME
.B _printf
- A simple re-creation of the printf function
.SH SYNOPSIS
.B #include "main.h"
.br
.I int _printf(const char *format, ...);
.SH DESCRIPTION
The
.B _printf
function produces output according to a format string.
It is a re-creation of the standard printf function. It supports the following format specifiers:
.RS 3
.B %c:
Used to print characters.
.RS
.B %s:
Used to print strings.
.RS
.B %d, %i:
Used to print integers, including the maximum and minimum values for an int type.
.RS
.B %b:
Used to print an integer in binary form.
.SH RETURN VALUES
The
.B _printf
function returns the number of characters printed, excluding the null terminator (\fI\0\fP), or -1 if an error occurs.
.SH SEE ALSO
.B printf(3)
.SH BUGS
No known bugs.
.SH AUTHOR
SALMA ERRAJI, Bill Mecha

